# Legacy / Prototype Scripts

These files are early-stage Python prototypes from the initial project design phase.
They are **not GenLayer intelligent contracts** — they have no runner dependency header,
no `from genlayer import *`, and cannot be deployed to a GenLayer network.

The single deployable GenLayer intelligent contract is:

```
contracts/trust_africa_intelligent_contract.py
```

These files are kept for project history only.
